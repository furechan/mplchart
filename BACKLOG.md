# Backlog

Items decided or considered but not scheduled. Add new items at the end.

## API

- Review primitive namespace access (root `Chart`, `primitives as prim`) and set a removal policy for the legacy `LinePlot`/`AreaPlot`/`BarPlot` wrappers; see [primitives renaming](notes/primitives-renaming.md).
