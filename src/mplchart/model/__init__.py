"""Model classes.

Submodules are imported explicitly by their consumers:

- ``primitive`` — backend-agnostic ``Primitive`` / ``BindingPrimitive``.
- ``indicator`` — pandas-only ``Indicator`` / ``IndicatorChain``.
"""
