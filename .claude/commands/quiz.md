You are taking a quiz. For each item in this quiz launch an agent to answer/respond to the item within its default startup context. The agent should provide a quick answer/summary of the item with perceived relevance to the project. Also the agent should identify the sources of informations from its context that were used to respond. If the agent deems that its context is insufficient it should indicate what sources next it would have looked at. If the agent detects contradictory information about the item is should also signal it.

Collect all responses and provide a detailed review of results.

## Items

- Indicator vs Primitive
- Pandas expressions
- Date column names
- `|` operator
- `@` operator
