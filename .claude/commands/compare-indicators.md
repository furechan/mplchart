Compare the list of indicators in `mplchart.indicators` against the expressions in `mplchart.expressions` and report on mismatches.

```bash
uv run python scripts/compare-indicators.py
```

Report the output clearly, noting which mismatches (if any) look intentional versus which look like gaps worth filling.
