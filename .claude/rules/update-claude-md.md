Update `.claude/CLAUDE.md` automatically for routine, durable project knowledge — commands, conventions, workflows, dependencies — the same way memory files are managed.

Only ask the user when the finding is significant enough to warrant a separate document (e.g. a dedicated architecture note, a design decision doc, or something that doesn't fit neatly into CLAUDE.md).

Do not add ephemeral or task-specific details to CLAUDE.md. Reserve it for durable, project-wide knowledge that is more important or structural than what belongs in `.claude/memory/`.
