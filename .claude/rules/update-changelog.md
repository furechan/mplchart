When making significant changes to the codebase — including major refactorings, new features, API changes, removed functionality, or bug fixes — update `CHANGELOG.md` before committing.

Use the format already established in the file: a `## x.y.z` version heading followed by bullet points. Add entries under the most recent version heading at the top.
