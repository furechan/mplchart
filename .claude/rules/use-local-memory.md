Use `.claude/memory/MEMORY.md` as the memory index for this project. Write all memory files to `.claude/memory/`. Do not use the global memory path.

Use dashes (not underscores) in memory file names (e.g. `project-my-topic.md`).

Manage memory automatically — write, update, and remove memory files without asking for confirmation. Only ask the user when a finding is significant enough to warrant a separate document beyond the standard memory files.
